{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge",
            "request": "launch",
            "type": "pwa-msedge",
            "runtimeExecutable": "/usr/bin/microsoft-edge-stable",
            "url": "http://localhost:5555",
            "webRoot": "${workspaceFolder}"
        }
    ]
}